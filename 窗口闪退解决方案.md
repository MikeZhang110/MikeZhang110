# 快速上手 - 看这里！

## ⚠️ 如果双击 .bat 文件后窗口闪退

这是**最常见的问题**！

### 解决方法（100%有效）：

**不要双击！用这个方法：**

1. **按住 Shift 键**
2. **右键点击**文件夹空白处
3. 选择 **"在此处打开命令窗口"** 或 **"在终端中打开"**
4. 输入命令：
   ```
   START-HERE.bat
   ```
5. 按回车

**或者更简单的方法：**

1. 按 **Win + R**
2. 输入：`cmd`
3. 按回车，打开命令提示符
4. 输入：
   ```
   cd C:\你解压的路径\MikeZhang110
   START-HERE.bat
   ```
5. 按回车

这样窗口**绝对不会闪退**！

---

## 🎯 为什么会闪退？

**原因**：
- 脚本运行太快就结束了
- 或者出错后立即关闭
- Windows 默认行为就是执行完就关闭

**解决**：
- 用命令行运行（见上面）
- 或者我提供的新文件 `START-HERE.bat`（有多重防护）

---

## 📋 文件说明

**新创建的文件**（从新到旧）：

| 文件名 | 说明 | 会闪退吗？ |
|--------|------|-----------|
| **START-HERE.bat** | 最新版，多重防护 | ❌ 不会！ |
| TEST.bat | 英文版测试 | ⚠️ 可能 |
| CHECK.bat | 环境检查 | ⚠️ 可能 |
| BUILD.bat | 构建工具 | ⚠️ 可能 |

---

## ⚡ 现在立即这样做：

### 方法1：命令行运行（推荐）⭐⭐⭐⭐⭐

```cmd
1. 按 Win + R
2. 输入 cmd
3. 按回车
4. 输入以下命令（替换路径）：

cd C:\你的路径\MikeZhang110
START-HERE.bat
```

### 方法2：创建一个不会闪退的快捷方式

1. **右键桌面** → 新建 → 快捷方式
2. 位置输入：
   ```
   cmd /k "C:\你的路径\MikeZhang110\START-HERE.bat"
   ```
3. 命名为"测试"
4. 双击这个快捷方式

`/k` 参数让窗口保持打开！

---

## 🔍 如果还是看不到

**终极方法**：

1. 打开记事本
2. 输入：
   ```batch
   @echo off
   cmd /k C:\你的路径\MikeZhang110\START-HERE.bat
   ```
3. 另存为 `运行测试.bat`（保存类型选"所有文件"）
4. 双击这个新文件

---

## 💡 检查Java最简单的方法

不用任何脚本！

1. 按 **Win + R**
2. 输入：`cmd`
3. 输入：`java -version`
4. 按回车

**结果**：
- 显示版本号 → Java已安装 ✅
- 提示"不是内部命令" → 没有Java ❌

---

## 📦 如果已经确认有Java

直接运行构建：

```cmd
Win + R → cmd → 回车

cd C:\你的路径\MikeZhang110
gradlew.bat clean shadowJar
```

等待3-5分钟，就会生成便携版！

---

## 🆘 还是不行？

请告诉我：

1. **您用的什么方法运行？**
   - 直接双击？
   - 命令行？

2. **Java 安装了吗？**
   - 在 cmd 输入 `java -version` 的结果是什么？

3. **文件在哪个路径？**
   - 例如：C:\Downloads\MikeZhang110

告诉我这些信息，我会给您最精确的解决方案！

---

## 📌 记住

**双击 .bat 闪退是正常的 Windows 行为！**

**解决方法就是：用命令行运行！**
