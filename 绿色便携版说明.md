# TranslationPro 绿色便携版使用指南

## 什么是绿色便携版？

绿色便携版是一个**无需安装、双击即用、不写注册表**的版本，就像许多绿色软件一样，您可以：

- ✅ **不需要安装**：解压即用
- ✅ **不写注册表**：不在系统中留下任何痕迹
- ✅ **不需要管理员权限**：普通用户即可运行
- ✅ **完全便携**：可以放在U盘、移动硬盘上随身携带
- ✅ **数据随身走**：所有翻译记忆库和项目都在文件夹内

## 两个版本选择

### 版本1：完整便携版（推荐，约100MB）

**优点**：
- ✅ 包含Java运行环境，无需另外安装Java
- ✅ 真正的"双击即用"
- ✅ 可以在任何Windows 11/10电脑上运行
- ✅ 适合分享给他人使用

**创建方法**：
```cmd
双击运行: create-portable.bat
```

**注意**：需要在有JDK的电脑上创建，但创建后可以在任何电脑使用。

---

### 版本2：轻量便携版（约30MB）

**优点**：
- ✅ 体积小，只有30MB左右
- ✅ 下载快，传输方便
- ✅ 功能完全相同

**缺点**：
- ⚠️ 需要电脑上已安装Java 17或更高版本

**创建方法**：
```cmd
双击运行: create-portable-simple.bat
```

---

## 快速开始（三步走）

### 第一步：创建便携版

根据您的需求选择：

**方案A - 完整版（推荐）**：
1. 双击 `create-portable.bat`
2. 等待构建完成（首次需要2-3分钟）
3. 会生成 `TranslationPro-Portable` 文件夹

**方案B - 轻量版**：
1. 双击 `create-portable-simple.bat`
2. 等待构建完成
3. 会生成 `TranslationPro-Portable-Lite` 文件夹

### 第二步：使用便携版

#### 完整版使用方法：
```
TranslationPro-Portable/
├── TranslationPro.bat  ← 双击这个文件启动！
├── jre/               （内置Java运行环境）
├── app/               （应用程序）
├── data/              （您的数据）
└── logs/              （日志文件）
```

**直接双击 `TranslationPro.bat` 即可启动**，无需任何额外操作！

#### 轻量版使用方法：

**前提**：确保已安装Java 17

检查Java：
```cmd
java -version
```

如果显示版本号（17或更高），可以直接使用。
如果提示"不是内部或外部命令"，需要先安装Java。

**Java下载地址**：https://adoptium.net/temurin/releases/
- 选择：Windows
- 版本：17 - LTS
- 类型：JDK
- 下载.msi文件，安装时勾选"Add to PATH"

然后：
```
TranslationPro-Portable-Lite/
├── 启动 TranslationPro.bat  ← 双击这个文件启动！
├── TranslationPro.jar       （应用程序）
├── data/                    （您的数据）
└── logs/                    （日志文件）
```

### 第三步：复制到任何位置使用

创建好的便携版文件夹可以：

- 📁 复制到桌面
- 💾 复制到D盘、E盘等任何位置
- 🔌 复制到U盘随身携带
- 💿 复制到移动硬盘
- ☁️ 放到网盘同步（需要注意不要在多台电脑同时使用同一个数据库）
- 📧 打包成ZIP分享给他人

**重要**：整个文件夹是一个整体，不要删除里面的任何文件。

---

## 详细使用说明

### 启动程序

**完整版**：
- 双击 `TranslationPro.bat`
- 程序会自动使用内置的Java运行环境启动
- 无需任何配置

**轻量版**：
- 双击 `启动 TranslationPro.bat` 或 `Start TranslationPro.bat`
- 如果未安装Java，会提示下载安装
- 安装Java后即可正常使用

### 数据存储位置

所有数据都保存在便携版文件夹内：

```
TranslationPro-Portable/
│
├── data/
│   ├── translationpro.mv.db  （翻译记忆库数据库）
│   ├── projects/              （您的翻译项目）
│   └── tm/                    （翻译记忆库文件）
│
└── logs/
    └── translationpro.log     （运行日志）
```

**优点**：
- ✅ 所有数据集中在一个文件夹
- ✅ 备份时只需复制整个文件夹
- ✅ 迁移时移动整个文件夹即可
- ✅ 不会在系统其他位置留下任何文件

### 创建桌面快捷方式

如果想从桌面快速启动：

**方法1 - 发送快捷方式**：
1. 右键点击 `TranslationPro.bat`
2. 选择"发送到" → "桌面快捷方式"
3. 完成！双击桌面上的快捷方式即可启动

**方法2 - 创建快捷方式**：
1. 在桌面上右键 → 新建 → 快捷方式
2. 浏览并选择 `TranslationPro.bat`
3. 命名为"TranslationPro"
4. 完成

### 多台电脑使用

**场景1 - 在多台电脑上使用相同数据**：
1. 将整个便携版文件夹复制到U盘
2. 在任何电脑上插入U盘
3. 运行便携版
4. 所有翻译记忆和项目都会跟随您

**场景2 - 在不同电脑上独立使用**：
1. 复制便携版到每台电脑
2. 各自使用，数据独立

**注意**：不要在多台电脑上同时打开同一个数据库文件，可能导致数据损坏。

---

## 常见问题

### Q1: 启动时闪退怎么办？

**轻量版**：
- 可能是Java版本太低或未安装
- 运行 `java -version` 检查版本
- 需要Java 17或更高版本

**完整版**：
- 检查 `logs/translationpro.log` 查看错误信息
- 确保没有杀毒软件阻止运行

### Q2: 可以改名吗？

可以！整个文件夹可以随意改名，不影响使用。

例如：
- `TranslationPro-Portable` → `我的翻译工具`
- `TranslationPro-Portable-Lite` → `TranslationPro`

### Q3: 可以同时运行多个吗？

可以！如果您复制了多份便携版到不同位置，可以同时运行，数据互不影响。

### Q4: 如何备份数据？

**简单方法**：
复制整个便携版文件夹到安全位置（移动硬盘、网盘等）

**只备份数据**：
只复制 `data` 文件夹即可

### Q5: 如何更新到新版本？

**方法1 - 保留数据**：
1. 复制旧版本的 `data` 文件夹
2. 下载新版本的便携版
3. 将 `data` 文件夹复制到新版本中
4. 删除旧版本（可选）

**方法2 - 直接替换程序**：
- 完整版：替换 `app/TranslationPro.jar`
- 轻量版：替换 `TranslationPro.jar`

### Q6: 占用多少空间？

- **完整版**：约100MB（包含JRE）
- **轻量版**：约30MB（不含JRE）
- **数据文件**：根据使用情况增长
  - 空白数据库：< 1MB
  - 中等使用（10万翻译单元）：约50MB
  - 重度使用（100万翻译单元）：约500MB

### Q7: 可以在Mac或Linux上用吗？

**完整版**：
- Windows专用，因为内置Windows版JRE
- 如需Mac/Linux版本，需要重新构建

**轻量版**：
- 理论上可以，如果该系统已安装Java
- 但启动脚本需要修改（.bat改为.sh）

### Q8: 会不会中毒或不安全？

- ✅ 完全开源，代码透明
- ✅ 不联网（除非您使用机器翻译功能）
- ✅ 不收集任何数据
- ✅ 不写注册表
- ✅ 所有数据保存在本地

建议从官方GitHub下载，不要从不明来源下载。

---

## 高级技巧

### 自定义Java内存

如果处理大型项目，可能需要增加内存：

编辑 `TranslationPro.bat`（或启动脚本），找到启动命令，修改为：

```batch
# 完整版
start jre\bin\javaw.exe -Xmx4096m -jar "app\TranslationPro.jar"

# 轻量版
start javaw -Xmx4096m -jar "TranslationPro.jar"
```

`-Xmx4096m` 表示最大使用4GB内存，可以根据需要调整。

### 配置文件位置

应用程序配置在：
```
app/TranslationPro.jar (内嵌)
```

如需修改配置，可以创建外部配置文件（高级用户）。

### 查看日志

如果遇到问题，查看日志文件：
```
logs/translationpro.log
```

用记事本打开即可查看详细的运行日志。

---

## 与他人分享

### 分享完整版（推荐）

1. 将整个 `TranslationPro-Portable` 文件夹压缩成ZIP
2. 可以通过网盘、邮件等方式分享
3. 对方解压后即可直接使用，无需安装任何东西

### 分享轻量版

1. 将 `TranslationPro-Portable-Lite` 压缩成ZIP
2. 分享时说明需要Java 17
3. 提供Java下载链接：https://adoptium.net/temurin/releases/

---

## 总结

TranslationPro绿色便携版的特点：

| 特性 | 完整版 | 轻量版 |
|------|--------|--------|
| 大小 | ~100MB | ~30MB |
| 需要Java | ❌ 不需要 | ✅ 需要 |
| 双击即用 | ✅ | ✅ |
| 写注册表 | ❌ | ❌ |
| 需安装 | ❌ | ❌ |
| 完全便携 | ✅ | ✅ |
| 适合分享 | ✅ 最佳 | ⚠️ 需说明 |

**推荐**：
- 如果要分享给他人：选择**完整版**
- 如果自己使用且已有Java：选择**轻量版**
- 如果要放U盘随身带：选择**完整版**

---

## 获取帮助

- 项目主页：https://github.com/MikeZhang110/MikeZhang110
- 问题报告：https://github.com/MikeZhang110/MikeZhang110/issues
- 完整文档：查看 `README.md` 和 `INSTALLATION.md`

---

**享受使用TranslationPro！** 🌍✨

一个真正的绿色软件，专业的翻译工具！
